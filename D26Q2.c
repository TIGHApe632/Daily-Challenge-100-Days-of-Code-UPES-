#include <stdio.h>

int main() {
    int rows = 10; // You can change this value for more or fewer asterisks

    // Loop to print an asterisk on each line
    for (int i = 0; i < rows; i++) {
        printf("*\n");
    }
    return 0;
}